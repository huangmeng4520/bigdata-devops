{
  "template": {
    "name": "file-server-dev",
    "code": "file-server-dev",
    "language": "java",
    "language_version": "17",
    "framework": "springboot",
    "description": "文件服务",
    "git_url": "ssh://git@220.163.62.194:12002/common/dsj-file-starter.git"
  },
  "version": {
    "version": "1.0.3",
    "content": "pipeline {\n    agent any\n\n    environment {\n        GROUP_NAME = null\n        PROJECT_NAME = null\n        VERSION = null\n        PORT=11014\n        REMOTE_HOST = '20.48.1.66' // 远程服务器地址\n        REMOTE_USER = 'root' // 远程服务器用户名\n        REMOTE_CREDENTIALS_ID = 'jenkins-server-ssh-private-key' // Jenkins中保存的远程服务器SSH凭证ID\n        TIMESTAMP = new Date().format(\"yyyyMMddHHmmss\", TimeZone.getTimeZone('UTC'))\n    }\n\n    stages {\n        stage('Git') {\n            steps {\n                echo \"Trying to checkout code from gitlab branch: ${params.Branch}\"\n                checkout scmGit(branches: [[name: \"${params.Branch}\"]], extensions: [], userRemoteConfigs: [[credentialsId: 'SSH-KEY-FOR-GITLAB', url: '${GIALAB_ADDRESS}/common/dsj-file-starter.git']])\n            }\n        }\n                \n        // stage('SonarQube Analysis') {\n        //     steps {\n        //         script {\n        //             def pom = readMavenPom file: 'pom.xml'\n        //             GROUP_NAME = pom.groupId\n        //             PROJECT_NAME = pom.artifactId\n        //             VERSION = pom.version\n        //         }\n        //         withSonarQubeEnv('SonarQube-dev') {\n        //           sh \"mvn verify sonar:sonar -DskipTests -Dsonar.projectKey=${PROJECT_NAME} -Dsonar.projectName='${PROJECT_NAME}'\"\n        //         }\n        //     }\n        // }\n        \n        stage('Mvn Compile') {\n            steps {\n                script {\n                    def pom = readMavenPom file: 'pom.xml'\n                    GROUP_NAME = pom.groupId\n                    PROJECT_NAME = pom.artifactId\n                    VERSION = pom.version\n                    sh \"mvn clean\"\n                    sh \"mvn install -Dmaven.test.skip=true -Pdev\"\n                }\n            }\n        }\n\n\n        stage('Build Image') {\n            steps {\n                script {\n                    try {\n                        echo 'try to remove old image before build.'\n                        sh \"docker rmi ${GROUP_NAME}/${PROJECT_NAME}:${VERSION}\"\n                        echo 'old image(${GROUP_NAME}/${PROJECT_NAME}:${VERSION}) removed.'\n                    } catch (Exception e) {\n                        echo 'nothing to remove.'\n                    }\n                \n                    echo 'try to build the image.'\n                    sh \"docker build -t ${GROUP_NAME}/${PROJECT_NAME}:${VERSION} -f \\\"devops/Dockerfile\\\" --build-arg group_name=${GROUP_NAME} --build-arg project_name=${PROJECT_NAME} --build-arg version=${VERSION} --build-arg port=${PORT} .\"\n                    echo 'image(${GROUP_NAME}/${PROJECT_NAME}:${VERSION}) is built.'\n                }\n            }\n        }\n        \n        \n        stage('Push Image') {\n            steps {\n                script {\n                    try{\n                    def timestamp = new Date().format(\"yyyyMMddHHmmss\", TimeZone.getTimeZone('UTC'))\n                        sh \" docker login --username=${DOCKER_REGISTRY_USERNAME} --password=${DOCKER_REGISTRY_PASSWORD} ${DOCKER_REGISTRY}\"\n                        sh \" docker tag ${GROUP_NAME}/${PROJECT_NAME}:${VERSION} ${DOCKER_REGISTRY}/${PROJECT_NAME}:${VERSION}-${env.TIMESTAMP}\"\n                        sh \" docker push ${DOCKER_REGISTRY}/${PROJECT_NAME}:${VERSION}-${env.TIMESTAMP}\"\n                        echo 'image(${GROUP_NAME}/${PROJECT_NAME}:${VERSION}-${env.TIMESTAMP}) is pushed to the registry.'\n                    } catch (Exception e) {\n                        echo 'push image failed'\n                        echo \"Caught exception: ${e.getMessage()}\"\n                    }\n                }\n            }\n        }\n\n        stage('Deploy to Remote') {\n            steps {\n                script {\n                    if (params.DEPLOY) {\n                        sshagent([REMOTE_CREDENTIALS_ID]) {\n                            sh \"\"\"\n                                ssh ${REMOTE_USER}@${REMOTE_HOST} '\n                                docker login --username=${DOCKER_REGISTRY_USERNAME} --password=${DOCKER_REGISTRY_PASSWORD} ${DOCKER_REGISTRY}  &&\n                              \n                                docker pull ${DOCKER_REGISTRY}/${PROJECT_NAME}:${VERSION}-${env.TIMESTAMP} &&\n                                \n                                docker stop ${PROJECT_NAME} || true &&\n                                \n                                docker rm ${PROJECT_NAME} || true &&\n                                        \n                                docker run -d  --add-host gxzcptdev.oss-cn-kunming-ynzwy01-d01-a.ops.amscyzx.zwy.yn.cegn.cn:20.12.18.27 --add-host oss-cn-kunming-ynzwy01-d01-a.ops.amscyzx.zwy.yn.cegn.cn:20.12.18.27  --name ${PROJECT_NAME} -p ${PORT}:${PORT} -m 512m -e TZ=Asia/Shanghai ${DOCKER_REGISTRY}/${PROJECT_NAME}:${VERSION}-${env.TIMESTAMP} \n                                '\n                               \"\"\"\n                               echo 'sh complete!'\n                        }\n                    } else {\n                        echo 'deploy skipped.'\n                    }\n                }\n            }\n        }\n    }\n\n\n}",
    "variables": {},
    "stages": [],
    "stages_content": {}
  }
}