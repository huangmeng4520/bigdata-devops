{
  "template": {
    "name": "result-transformation-algorithm-dev",
    "code": "result-transformation-algorithm-dev",
    "language": "java",
    "language_version": "17",
    "framework": "springboot",
    "description": "科技成果转化平台算法服务-dev",
    "git_url": "ssh://git@220.163.62.194:12002/dengbin/result-transformation-algorithm.git"
  },
  "version": {
    "version": "1.0.3",
    "content": "pipeline {\n    agent any\n\n    parameters {\n        string(name: 'Branch', defaultValue: 'main', description: 'Git branch to build')\n        booleanParam(name: 'DEPLOY', defaultValue: false, description: 'Deploy to remote server after build')\n    }\n\n    environment {\n        GROUP_NAME = 'com.dsj'\n        PROJECT_NAME = 'result-transformation-algorithm'\n        VERSION = '1.0.0'\n        PORT = '30015'\n        REMOTE_HOST = '20.48.1.67'\n        REMOTE_USER = 'root'\n        REMOTE_CREDENTIALS_ID = 'jenkins-server-ssh-private-key'\n        TIMESTAMP = new Date().format(\"yyyyMMddHHmmss\", TimeZone.getTimeZone('Asia/Shanghai'))\n        // 以下变量需在 Jenkins 全局或文件夹级别定义为凭据/环境变量\n        // DOCKER_REGISTRY, DOCKER_REGISTRY_USERNAME, DOCKER_REGISTRY_PASSWORD, GIALAB_ADDRESS\n    }\n\n    stages {\n        stage('Git Checkout') {\n            steps {\n                echo \"Checking out code from branch: ${params.Branch}\"\n                checkout scmGit(\n                    branches: [[name: \"${params.Branch}\"]],\n                    extensions: [\n                        [$class: 'GitLFSPull']   // 添加此扩展以支持 LFS\n                    ],\n                    userRemoteConfigs: [[\n                        credentialsId: 'SSH-KEY-FOR-GITLAB',\n                        url: \"${GIALAB_ADDRESS}/dengbin/result-transformation-algorithm.git\"\n                    ]]\n                )\n            }\n        }\n\n        stage('Build Image') {\n            steps {\n                script {\n                    def imageTag = \"${GROUP_NAME}/${PROJECT_NAME}:${VERSION}\"\n\n                    // 删除旧镜像（如果存在）\n                    sh script: \"docker rmi ${imageTag} || true\", label: \"Remove old image\"\n\n                    echo 'Building Docker image...'\n                    sh \"\"\"\n                        docker build --no-cache -t ${imageTag} \\\\\n                            -f \"devops/Dockerfile\" \\\\\n                            --build-arg group_name=${GROUP_NAME} \\\\\n                            --build-arg project_name=${PROJECT_NAME} \\\\\n                            --build-arg version=${VERSION} \\\\\n                            --build-arg port=${PORT} .\n                    \"\"\"\n                    echo \"Docker image ${imageTag} built successfully\"\n                }\n            }\n        }\n\n        stage('Push Image') {\n            steps {\n                script {\n                    try {\n                        sh \"\"\"\n                            echo \"Logging into Docker registry...\"\n                            docker login --username=${DOCKER_REGISTRY_USERNAME} --password=${DOCKER_REGISTRY_PASSWORD} ${DOCKER_REGISTRY}\n\n                            echo \"Tagging image...\"\n                            docker tag ${GROUP_NAME}/${PROJECT_NAME}:${VERSION} ${DOCKER_REGISTRY}/${PROJECT_NAME}:${VERSION}-${TIMESTAMP}\n\n                            echo \"Pushing image to registry...\"\n                            docker push ${DOCKER_REGISTRY}/${PROJECT_NAME}:${VERSION}-${TIMESTAMP}\n\n                            echo \"Image pushed to registry: ${DOCKER_REGISTRY}/${PROJECT_NAME}:${VERSION}-${TIMESTAMP}\"\n                        \"\"\"\n                    } catch (Exception e) {\n                        echo 'push image failed'\n                        echo \"Caught exception: ${e.getMessage()}\"\n                        // 可以根据需要决定是否继续或抛出错误\n                        // 此处仅记录，不中断流程\n                    }\n                }\n            }\n        }\n\n        stage('Deploy to Remote') {\n            when {\n                expression { return params.DEPLOY }\n            }\n            steps {\n                script {\n                    def imageName = \"${DOCKER_REGISTRY}/${PROJECT_NAME}:${VERSION}-${TIMESTAMP}\"\n\n                    sshagent([REMOTE_CREDENTIALS_ID]) {\n                        sh \"\"\"\n                            ssh -o StrictHostKeyChecking=no ${REMOTE_USER}@${REMOTE_HOST} '\n                                set -e\n                                echo \"Starting deployment of ${imageName}\"\n\n                                echo \"Logging into Docker registry...\"\n                                docker login --username=${DOCKER_REGISTRY_USERNAME} \\\\\n                                --password=${DOCKER_REGISTRY_PASSWORD} ${DOCKER_REGISTRY}\n\n                                echo \"Pulling image...\"\n                                docker pull ${imageName}\n\n                                echo \"Stopping and removing existing container...\"\n                                docker rm -f ${PROJECT_NAME} || true\n\n                                echo \"Starting new container...\"\n                                docker run -d \\\\\n                                    --name ${PROJECT_NAME} \\\\\n                                    -p ${PORT}:${PORT} \\\\\n                                    -e APP_ENV=dev \\\\\n                                    -e TZ=Asia/Shanghai \\\\\n                                    ${imageName}\n\n                                echo \"Deployment completed successfully\"\n                            '\n                        \"\"\"\n                    }\n                }\n            }\n        }\n    }\n\n    // 可选：构建后清理\n    post {\n        always {\n            echo \"Pipeline finished at ${new Date()}\"\n        }\n    }\n}",
    "variables": {},
    "stages": [],
    "stages_content": {}
  }
}